ADDA	START
	LAD	GR0, 32760
	LAD	GR1, 100
	ADDA	GR0, GR1	; ? OF==1 GR0==0x805c
	ADDA	GR0, ONE	; ? GR0==0x805d
	ADDA	GR0, NEG	; ? GR0==0x805b
	ADDA	GR0, V		; ? SF==0 ZF==1
	RET
ONE	DC	1	
NEG	DC	-2
V	DC	32677
	END
